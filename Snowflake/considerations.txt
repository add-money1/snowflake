Dokumentation für Klassen:
	- thread-safety
	- open-/closeable
	
	
	LOGGER?
	
	StorageException -> wo muss diese Exception geworfen, und wo muss sie abgefangen werden?
	
	
Klasse:
	- Flake:
		-> bekommt IOExceptions von closeAllStreams() -> was damit anfangen? (loggen??)
	- IChunkManager:
		-> sind die Implementationen von recycleChunks() thread-safe (bzgl. Streams -> dürfen nicht während der
				Ausführung manipuliert werden!)
	- FlakeStreamManager:
		-> removeStream() -> was soll hier noch zusätzlich geschehen? Listener könnten hier das Ende
							eines i/O-vorgangs signalisieren
	
	
Server:
	- FTP/eigene Schnittstelle/NFS/SMB?
	- Metadaten für Dateien
	- Kompatibilität mit java.io.File?
	- mehrere Flakes pro Datei?
	- Ordner, oder Links, oder beides?
	- sind Ordner auch Dateien?
	- 